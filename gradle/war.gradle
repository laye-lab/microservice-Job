apply plugin: "war"

bootWar {
    mainClassName = "com.cours.MicroserviceJobApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
